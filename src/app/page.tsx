import GreetingApp from "@/components/GreetingApp";

export default function Home() {
  return (
    <main className="min-h-screen bg-[#0a0a0a] text-neutral-100 overflow-hidden relative">
      <GreetingApp />
    </main>
  );
}
